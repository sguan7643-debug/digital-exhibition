# PRD — 101项真实接口联调

## Scope

逐项验证 operation registry 的 101 个操作，不以契约、单测或构建替代真实网络结果。65项读取需要记录真实请求和响应；36项写入只能操作 TEST_ 记录并完成清理。附件、幂等、版本冲突和回滚需要独立证据。

## Acceptance

每个 operation ID 必须标记为 passed、failed 或 blocked，并附真实请求、上游结果、表中结果及清理结果。可由本仓库解决的 failed 项进入受控修复；外部缺件必须明确到系统、权限、账号、回调或字段。
