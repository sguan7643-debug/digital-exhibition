# UI Handoff — Test Deployment Package

Sources: approved `prd.md` and `design/ux-handoff.md`.

No visual-system, component, responsive, typography, color, spacing, motion, state-presentation or accessibility changes are required. The generated package must preserve the existing Vue output byte-for-byte relative to the normal build. Web is the only declared platform; uni-app visual adaptation is not applicable.
