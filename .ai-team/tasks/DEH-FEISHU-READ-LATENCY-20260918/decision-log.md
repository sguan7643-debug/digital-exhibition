# 决策日志

## 2026-09-19 / 范围批准

- 决策：Product Owner 明确批准范围 `DEH-FEISHU-READ-LATENCY-20260918`。
- 记录：治理脚本已原子记录 scope approval，并将任务推进至 `design`。
- 已确认基线：五个 P0 首屏接口热态 p95 不超过 2 秒；其余 60 个读取 operation 合同正确且 504 为 0；无持久业务快照的真正冷启动在 2 秒内返回可恢复的首次同步状态；强一致读取不使用陈旧缓存。

## 2026-09-18 / 立项

- 决策：性能闭环作为独立任务处理。
- 原因：用户把既有 8–12 秒有界返回目标收紧到 1–2 秒，属于新增验收标准。
- 已排除：请求全部串行、代理是主要瓶颈。
- 已确认：飞书单表实时读取、2 秒短缓存、按字段拆分缓存和复合读取阶段是主要延迟来源。
- 安全约束：正式源仍为飞书；强一致接口不得返回陈旧状态；不得记录密钥、令牌、Cookie 或未脱敏个人信息。
